interface:
  display_name: "Priors"
  short_description: "Repo-specific patterns and workflows for priors"
  default_prompt: "Use the priors repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true
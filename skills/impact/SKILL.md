---
description: Show whether Priors helped this session — pushbacks made, rules applied, candidates proposed.
---

Run `node ${CLAUDE_PLUGIN_ROOT}/bin/priors.js impact --project-root "${CLAUDE_PROJECT_DIR:-$PWD}"` and present the result.

The report is text-only and already framed for non-technical readers — present it as-is. Do not pad with commentary.
